#include "main.h"
/**
 * _strlen - length of the string will be returned
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int longi = 0;
	
	while (*s l = '\0');
	{
		longi++;
		s++;
	}
	return (longi);
}
